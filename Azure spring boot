#Test you app locally with maven
1. mvn package spring-boot:run

============follow the steps on azure note untill step 10=============

#Create a private Azure container registry in the resource group
az acr create --admin-enabled --resource-group java-microservice-resource-group-aks --location eastus --name javamicroserviceregistry --sku Basic
