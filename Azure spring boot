#Test you app locally with maven
1. mvn package spring-boot:run

============follow the steps on azure note untill step 10=============

#Create a private Azure container registry in the resource group
2. az acr create --admin-enabled --resource-group java-microservice-resource-group-aks --location eastus --name javamicroserviceregistry --sku Basic

#Retrieve the password for your container registry from the Azure CLI.
3. az acr credential show --name javamicroserviceregistry  --resource-group java-microservice-resource-group-aks --query passwords[0]

#view the kubernetes portal
4. az aks browse --name=cluster1 --resource-group java-microservice-resource-group-aks 
